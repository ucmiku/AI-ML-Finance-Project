[theme]
# 主题色（Logo 中的青绿色）：影响按钮、滑块、选中状态、加载条等
primaryColor = "#10B981" 

# 背景色：整个应用的最底层背景（浅灰色，让内容区显得突出）
backgroundColor = "#F4F5F7" 

# 次要背景色：侧边栏和交互组件的底色（纯白）
secondaryBackgroundColor = "#FFFFFF" 

# 文本与字体
textColor = "#1E293B"
font = "sans serif"