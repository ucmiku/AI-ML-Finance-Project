# Required by the FRED collector. Never commit a real key here.
FRED_API_KEY=

# Required by the GridStatus.io hosted API collector.
GRIDSTATUS_API_KEY=

# Open-Meteo's public API normally does not require a key. Set this only when
# using a customer API plan.
OPENMETEO_API_KEY=
